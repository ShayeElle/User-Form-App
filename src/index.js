import React from 'react';
import ReactDOM from 'react-dom/client';

import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);

// the first code file that is first executed when the oage is loades

// initializes a React root using the createRoot method from the ReactDOM package and renders the <App /> component inside the root

