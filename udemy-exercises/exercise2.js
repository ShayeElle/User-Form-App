import React from 'react';

    // don't change the Component name "App"
    export default function App() {
        return (
            <div>
                <h1> Exercise done!</h1>
                <p>Practicing React...</p>
            </div>
        );
    }

    // With JSX we can use HTML elements in our code such as <h1> or <p>